[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "insightforge-ml"
version = "1.0.0"
description = "InsightForge ML: dependency-light forecasting and anomaly detection over governed datasets"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["insightforge_ml*"]
